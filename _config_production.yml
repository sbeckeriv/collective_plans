# Production configuration overrides
# This file is merged with _config.yml during GitHub Actions deploy

pagination:
  enabled: true
  collection: pinball
  per_page: 24  # Production: 24 machines per page
  sort_field: title
  sort_reverse: false
  trail:
    before: 2
    after: 2
